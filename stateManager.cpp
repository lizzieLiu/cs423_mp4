#include <stdio.h>
#include "stateManager.h"

#define DEBUG 1

stateManager::stateManager() {
  if ( DEBUG )
    printf("===init stateManager===\n");

}

void 
stateManager::run() {
  if ( DEBUG )
    printf("===init stateManager===\n");
}
